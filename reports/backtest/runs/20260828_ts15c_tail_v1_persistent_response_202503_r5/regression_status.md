# Regression status

`VALID_FOR_STEP15C_DEVELOPMENT_ANALYSIS`

The frozen Step 15B detector features and strategy funnel match after excluding
run-identity fields. Event-response accounting is 21,799 rows, 10,245 market
clusters, with zero horizon-time violations, response drops, invalid rows, or
duplicate event rows. The authoritative comparison is
`reports/analysis/tick_shock/step15c/detector_identity_regression.csv`.
