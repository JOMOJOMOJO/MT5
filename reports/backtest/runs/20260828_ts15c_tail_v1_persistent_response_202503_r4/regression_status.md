# Regression status

`INVALID_FOR_STEP15C_ANALYSIS`

This run retained legacy statistical and counterfactual state for the longer
event-response lifetime. That changed legacy forward diagnostics and admitted
one post-burst fill that the frozen Step 15B lifecycle had already closed. The
`_r5` implementation freezes legacy state at its original first 120-second grid
boundary while retaining only the independent event-response recorder.
