# Superseded STRICT_V0 diagnostic run

This completed run used source commit `dd3048bb` before the statistical-event
storage remediation. Its frozen STRICT_V0 counts matched Step 14R, but it is not
the same source/EX5 as the three formal V1 comparisons. It is retained as
development history and superseded by
`20260827_ts15a_strict_v0_realizable_202503_r2`, which uses the same commit and
EX5 as all formal Step 15A comparison runs.
