# Regression status

`INVALID_FOR_STEP15C_ANALYSIS`

This second run restored the post-burst counts but closed the extended track
before one historical terminal-boundary reversal state transition. It produced
7,281 reachable reversals and 7,280 causal reversal fills instead of the frozen
7,282 / 7,281 baseline. The `_r3` run separates state-transition observation
from the 120-second entry window and is the next authoritative candidate.
