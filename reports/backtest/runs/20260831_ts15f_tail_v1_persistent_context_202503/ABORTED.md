# Aborted Step 15F run

This directory is not formal evidence. The run was interrupted before completion after identifying an unused cross-symbol USD-factor calculation on every tick. No outcome was analyzed. The behavior-preserving performance correction was applied before the formal `r2` run.
