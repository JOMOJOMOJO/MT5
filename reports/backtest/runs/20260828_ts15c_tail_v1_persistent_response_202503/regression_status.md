# Regression status

`INVALID_FOR_STEP15C_ANALYSIS`

This first recorder run preserved detector identity but extended the shared
statistical-track lifetime while waiting for the first post-120-second quote.
That unintentionally allowed one additional post-burst fill and one additional
reversal fill after the pre-existing 120-second strategy window. The run is
retained as negative regression evidence and must not be used for Discovery or
Internal confirmation. The corrected run uses the `_r2` RunId and directory.
