# 知識ベース方針

- 単なる日報は `.company/` に置く。
- 後で検索して再利用したいものは `knowledge/` に置く。
- `backtests` は事実の記録。
- `experiments` は仮説と次の検証。
- `lessons` は失敗や発見からの教訓。
- `patterns` は複数回効いた再利用可能な型。

# 良い記録の条件

- 何を試したかが分かる。
- どの条件で試したかが分かる。
- 何が良くて何が悪かったかが分かる。
- 次に何をするかが分かる。
