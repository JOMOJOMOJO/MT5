# Tick-shock Step 12: fail-closed integrity

Step 11 converted independent audit findings into executable RED evidence.
Step 12 fixed production boundaries without changing the strategy grid or
oracle. Missing commission denominators, capacity loss, cursor stall,
stale/incomplete frontier and ambiguous run identity now invalidate research
evidence instead of silently producing a normal partial run.

Evidence: `reports/tests/tick_shock/step12_post_fix_green_report.md` and
`reports/qa/tick_shock/step12_fixture_integrity.csv`.
